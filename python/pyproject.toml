# Точка входа всегда main.py (её задаёт Runner). Только stdlib: sandbox без сети.
[project]
name = "catalog-dedup"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

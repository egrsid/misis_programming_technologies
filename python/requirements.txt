# Зависимости решения на Python.
#
# ВАЖНО: sandbox Runner'а сетеизолирован — `pip install` из интернета там НЕ
# работает. Сюда можно добавлять только те пакеты, которые заранее впечены в
# образ `runner-python`. Базовый шаблон решается на stdlib (json), поэтому файл
# пуст. Локально для самопроверки достаточно интерпретатора Python 3.11+.
#
# Пример (если преподаватель впёк пакет в образ Runner'а):
#   scikit-learn==1.5.2
